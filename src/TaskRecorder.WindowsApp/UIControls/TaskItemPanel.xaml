<UserControl x:Class="TaskRecorder.WindowsApp.UIControls.TaskItemPanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TaskRecorder.WindowsApp.UIControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Style>
            <Style TargetType="Grid">
                <Setter Property="Background" Value="Transparent"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background" Value="LightBlue"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
        </Grid.Style>
        <StackPanel Margin="10px">
            <TextBlock x:Name="TaskNameLabel" Text="Task name" FontWeight="Bold" FontSize="16"/>
            <TextBlock x:Name="TaskDescriptionLabel" Text="Task descriotion" FontSize="12"/>
        </StackPanel>
    </Grid>
</UserControl>
